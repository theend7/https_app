_Make sure you are inside certs dir, **chmod +x gencerts.sh** , then **./gencerts.sh**_ 
then **npm run app** _(make sure you are in app_https dir, cert.pem and key.pem will be generated by shell script)_